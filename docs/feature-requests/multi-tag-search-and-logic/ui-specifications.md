# UI 设计规格文档

## 🎨 设计概览

### 设计原则
- **一致性**: 与现有 AFFiNE 设计语言保持一致
- **简洁性**: 避免过度复杂的界面
- **直观性**: 用户无需学习即可使用
- **响应式**: 支持桌面端和移动端

---

## 📱 界面设计

### 方案 A: 文档列表标签过滤器

#### 1. 过滤器栏设计
```
┌─────────────────────────────────────────────────────────┐
│ 📁 All Documents                                      ⚙️│
├─────────────────────────────────────────────────────────┤
│ 🔍 Search...  │ 🏷️ Tags: [工作] [重要] [+]           │
├─────────────────────────────────────────────────────────┤
│ 📄 Document 1                              工作 重要    │
│ 📄 Document 2                              工作         │
│ 📄 Document 3                              重要 个人    │
└─────────────────────────────────────────────────────────┘
```

#### 2. 标签选择器
```
┌─────────────────────────────────┐
│ 选择标签 (AND 逻辑)             │
├─────────────────────────────────┤
│ 🔍 搜索标签...                  │
├─────────────────────────────────┤
│ ☑️ 工作 (25)                    │
│ ☑️ 重要 (12)                    │
│ ☐ 个人 (8)                     │
│ ☐ 项目 (15)                    │
│ ☐ 会议 (6)                     │
├─────────────────────────────────┤
│ [清除全部] [确定] [取消]         │
└─────────────────────────────────┘
```

### 方案 B: 快速搜索增强

#### 搜索语法提示
```
┌─────────────────────────────────────────┐
│ 🔍 tag:工作 tag:重要                    │
├─────────────────────────────────────────┤
│ 💡 搜索提示:                            │
│   • tag:标签名 - 按标签搜索             │
│   • 多个标签用空格分隔 (AND 逻辑)       │
│   • "内容" - 按内容搜索                 │
├─────────────────────────────────────────┤
│ 🏷️ 标签结果                            │
│   工作 (25 篇文档)                      │
│   重要 (12 篇文档)                      │
│ 📄 文档结果                            │
│   • Document 1 (工作, 重要)             │
│   • Document 2 (工作, 重要)             │
└─────────────────────────────────────────┘
```

---

## 🎛️ 交互设计

### 标签过滤器交互流程

#### 1. 添加标签
```
用户点击 [+] 按钮
    ↓
弹出标签选择器
    ↓
用户选择标签 (支持多选)
    ↓
实时显示过滤结果
    ↓
添加到当前过滤条件
```

#### 2. 移除标签
```
用户点击标签的 [×] 按钮
    ↓
从过滤条件中移除该标签
    ↓
实时更新搜索结果
```

#### 3. 搜索逻辑
```
AND 逻辑: 文档必须包含所有选中的标签
例: [工作] AND [重要] = 同时包含"工作"和"重要"标签的文档
```

---

## 🎨 视觉规范

### 色彩规范
- **主色调**: 沿用 AFFiNE 品牌色 `#6366F1`
- **标签背景**: `#F3F4F6` (浅灰)
- **标签文字**: `#374151` (深灰)
- **选中状态**: `#EEF2FF` (浅蓝)
- **高亮文字**: `#6366F1` (品牌色)

### 字体规范
- **标签文字**: 12px / 0.75rem
- **搜索框**: 14px / 0.875rem
- **提示文字**: 12px / 0.75rem
- **计数器**: 11px / 0.6875rem

### 间距规范
- **标签间距**: 4px
- **行间距**: 8px
- **组件内边距**: 8px
- **组件外边距**: 12px

### 圆角规范
- **标签圆角**: 4px
- **按钮圆角**: 6px
- **弹窗圆角**: 8px

---

## 📱 响应式设计

### 桌面端 (>= 1024px)
- 标签过滤器显示在文档列表上方
- 支持并排显示多个标签
- 鼠标悬停显示详细信息

### 平板端 (768px - 1023px)
- 标签过滤器可折叠
- 标签可换行显示
- 触摸友好的按钮尺寸

### 移动端 (< 768px)
- 标签过滤器折叠为下拉菜单
- 全屏标签选择器
- 底部固定操作按钮

---

## 🔧 组件规格

### TagFilter 组件
```typescript
interface TagFilterProps {
  selectedTags: Tag[];
  onTagsChange: (tags: Tag[]) => void;
  availableTags: Tag[];
  loading?: boolean;
  placeholder?: string;
  maxDisplayTags?: number;
}
```

### TagSelector 组件
```typescript
interface TagSelectorProps {
  tags: Tag[];
  selectedTags: Tag[];
  onSelect: (tag: Tag) => void;
  onDeselect: (tag: Tag) => void;
  searchable?: boolean;
  showCount?: boolean;
}
```

### FilterChip 组件
```typescript
interface FilterChipProps {
  tag: Tag;
  onRemove: () => void;
  color?: string;
  size?: 'small' | 'medium';
}
```

---

## 🎭 状态设计

### 空状态
```
┌─────────────────────────────────┐
│          🏷️                     │
│      还没有标签                  │
│   开始为文档添加标签吧            │
│                                 │
│     [创建第一个标签]             │
└─────────────────────────────────┘
```

### 加载状态
```
┌─────────────────────────────────┐
│ 🔍 Search...  │ 🏷️ Tags: ⏳    │
├─────────────────────────────────┤
│ ⏳ 正在搜索...                  │
└─────────────────────────────────┘
```

### 无结果状态
```
┌─────────────────────────────────┐
│          🔍                     │
│      未找到匹配的文档            │
│   尝试调整筛选条件或搜索关键词    │
│                                 │
│     [清除筛选条件]               │
└─────────────────────────────────┘
```

---

## 🎯 可用性考虑

### 无障碍设计
- 支持键盘导航 (Tab, Enter, Escape)
- 提供 ARIA 标签和角色
- 支持屏幕阅读器
- 高对比度模式兼容

### 性能优化
- 标签列表虚拟化 (>100 个标签时)
- 搜索防抖 (300ms)
- 结果缓存机制
- 懒加载非关键样式

### 用户体验
- 实时搜索反馈
- 清晰的视觉层次
- 一致的交互模式
- 适当的动画过渡

---

## 📐 技术实现规格

### CSS 变量
```css
:root {
  --tag-bg: #f3f4f6;
  --tag-text: #374151;
  --tag-selected: #eef2ff;
  --tag-hover: #e5e7eb;
  --tag-border-radius: 4px;
  --tag-padding: 4px 8px;
  --tag-margin: 2px;
}
```

### 动画规格
```css
/* 标签添加动画 */
.tag-enter {
  opacity: 0;
  transform: scale(0.8);
  transition: all 0.2s ease-out;
}

.tag-enter-active {
  opacity: 1;
  transform: scale(1);
}

/* 标签移除动画 */
.tag-exit {
  opacity: 1;
  transform: scale(1);
  transition: all 0.15s ease-in;
}

.tag-exit-active {
  opacity: 0;
  transform: scale(0.8);
}
```

---

**文档版本**: v1.0  
**创建时间**: 2025-01-25  
**审核状态**: 待审核