# B2 实时支援 B3 - 样式系统集成实施

## 🎯 当前支援状态

**时间**: 2025-01-27
**支援对象**: 开发者B3 - 样式工程师  
**支援任务**: 基于交互设计创建完整样式系统
**支援方式**: 实时代码生成 + 样式规范指导

## 🚀 立即实施的样式系统

### 1. 核心设计令牌系统

```typescript
// 文件: packages/components/src/shared/design-tokens.ts
export const DesignTokens = {
  // === 颜色系统 ===
  colors: {
    // 主要色彩
    primary: {
      50: '#eff6ff',
      100: '#dbeafe',
      500: '#3b82f6',
      600: '#2563eb',
      700: '#1d4ed8',
      900: '#1e3a8a'
    },
    
    // 中性色彩
    gray: {
      50: '#f9fafb',
      100: '#f3f4f6',
      200: '#e5e7eb',
      300: '#d1d5db',
      400: '#9ca3af',
      500: '#6b7280',
      600: '#4b5563',
      700: '#374151',
      800: '#1f2937',
      900: '#111827'
    },
    
    // 语义化颜色
    semantic: {
      success: '#22c55e',
      warning: '#f59e0b',
      error: '#ef4444',
      info: '#3b82f6'
    },
    
    // 交互状态颜色
    interaction: {
      hover: 'rgba(59, 130, 246, 0.1)',
      active: 'rgba(59, 130, 246, 0.2)',
      focus: '#3b82f6',
      disabled: 'rgba(107, 114, 128, 0.3)',
      selected: 'rgba(59, 130, 246, 0.15)'
    }
  },
  
  // === 间距系统 ===
  spacing: {
    0: '0px',
    1: '4px',
    2: '8px',
    3: '12px',
    4: '16px',
    5: '20px',
    6: '24px',
    8: '32px',
    10: '40px',
    12: '48px',
    16: '64px',
    20: '80px',
    24: '96px'
  },
  
  // === 字体系统 ===
  typography: {
    fontSize: {
      xs: '12px',
      sm: '14px',
      base: '16px',
      lg: '18px',
      xl: '20px',
      '2xl': '24px',
      '3xl': '30px'
    },
    
    fontWeight: {
      normal: '400',
      medium: '500',
      semibold: '600',
      bold: '700'
    },
    
    lineHeight: {
      tight: '1.25',
      normal: '1.5',
      relaxed: '1.75'
    },
    
    fontFamily: {
      sans: ['Inter', 'system-ui', 'sans-serif'],
      mono: ['JetBrains Mono', 'Menlo', 'monospace']
    }
  },
  
  // === 圆角系统 ===
  borderRadius: {
    none: '0px',
    sm: '4px',
    base: '6px',
    md: '8px',
    lg: '12px',
    xl: '16px',
    full: '9999px'
  },
  
  // === 阴影系统 ===
  boxShadow: {
    sm: '0 1px 2px 0 rgba(0, 0, 0, 0.05)',
    base: '0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05)',
    xl: '0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)',
    inner: 'inset 0 2px 4px 0 rgba(0, 0, 0, 0.06)',
    focus: '0 0 0 3px rgba(59, 130, 246, 0.5)'
  },
  
  // === 动画系统 ===
  animation: {
    duration: {
      fast: '150ms',
      normal: '300ms',
      slow: '500ms'
    },
    
    easing: {
      smooth: 'cubic-bezier(0.4, 0, 0.2, 1)',
      bounce: 'cubic-bezier(0.68, -0.55, 0.265, 1.55)',
      sharp: 'cubic-bezier(0.4, 0, 0.6, 1)'
    }
  },
  
  // === 断点系统 ===
  breakpoints: {
    sm: '640px',
    md: '768px',
    lg: '1024px',
    xl: '1280px',
    '2xl': '1536px'
  },
  
  // === Z-index 系统 ===
  zIndex: {
    hide: '-1',
    auto: 'auto',
    base: '0',
    docked: '10',
    dropdown: '1000',
    sticky: '1100',
    banner: '1200',
    overlay: '1300',
    modal: '1400',
    popover: '1500',
    skipLink: '1600',
    toast: '1700',
    tooltip: '1800'
  }
} as const;

// 生成CSS变量
export function generateCSSVariables() {
  const variables: Record<string, string> = {};
  
  // 颜色变量
  Object.entries(DesignTokens.colors.primary).forEach(([key, value]) => {
    variables[`--color-primary-${key}`] = value;
  });
  
  Object.entries(DesignTokens.colors.gray).forEach(([key, value]) => {
    variables[`--color-gray-${key}`] = value;
  });
  
  Object.entries(DesignTokens.colors.semantic).forEach(([key, value]) => {
    variables[`--color-${key}`] = value;
  });
  
  Object.entries(DesignTokens.colors.interaction).forEach(([key, value]) => {
    variables[`--color-interaction-${key}`] = value;
  });
  
  // 间距变量
  Object.entries(DesignTokens.spacing).forEach(([key, value]) => {
    variables[`--spacing-${key}`] = value;
  });
  
  // 字体变量
  Object.entries(DesignTokens.typography.fontSize).forEach(([key, value]) => {
    variables[`--font-size-${key}`] = value;
  });
  
  // 圆角变量
  Object.entries(DesignTokens.borderRadius).forEach(([key, value]) => {
    variables[`--border-radius-${key}`] = value;
  });
  
  // 阴影变量
  Object.entries(DesignTokens.boxShadow).forEach(([key, value]) => {
    variables[`--shadow-${key}`] = value;
  });
  
  // 动画变量
  Object.entries(DesignTokens.animation.duration).forEach(([key, value]) => {
    variables[`--duration-${key}`] = value;
  });
  
  Object.entries(DesignTokens.animation.easing).forEach(([key, value]) => {
    variables[`--easing-${key}`] = value;
  });
  
  return variables;
}
```

### 2. 样式工具函数库

```typescript
// 文件: packages/components/src/shared/style-utils.ts
import { css, CSSResult } from 'lit';
import { DesignTokens } from './design-tokens.js';

export class StyleUtils {
  
  /**
   * 响应式样式生成器
   */
  static responsive(breakpoint: keyof typeof DesignTokens.breakpoints, styles: CSSResult): CSSResult {
    return css`
      @media (min-width: ${DesignTokens.breakpoints[breakpoint]}) {
        ${styles}
      }
    `;
  }
  
  /**
   * 创建交互状态样式
   */
  static interactive(options: {
    base?: CSSResult;
    hover?: CSSResult;
    focus?: CSSResult;
    active?: CSSResult;
    disabled?: CSSResult;
  } = {}): CSSResult {
    return css`
      ${options.base || css``}
      
      &:hover:not(:disabled) {
        ${options.hover || css`
          background-color: var(--color-interaction-hover);
          transition: all var(--duration-fast) var(--easing-smooth);
        `}
      }
      
      &:focus-visible {
        ${options.focus || css`
          outline: 2px solid var(--color-interaction-focus);
          outline-offset: 2px;
        `}
      }
      
      &:active:not(:disabled) {
        ${options.active || css`
          background-color: var(--color-interaction-active);
          transform: translateY(1px);
        `}
      }
      
      &:disabled {
        ${options.disabled || css`
          opacity: 0.6;
          cursor: not-allowed;
          background-color: var(--color-interaction-disabled);
        `}
      }
    `;
  }
  
  /**
   * 创建布局容器样式
   */
  static container(maxWidth?: string): CSSResult {
    return css`
      width: 100%;
      margin-left: auto;
      margin-right: auto;
      padding-left: var(--spacing-4);
      padding-right: var(--spacing-4);
      
      ${maxWidth ? css`max-width: ${maxWidth};` : css``}
      
      ${this.responsive('sm', css`
        padding-left: var(--spacing-6);
        padding-right: var(--spacing-6);
      `)}
      
      ${this.responsive('lg', css`
        padding-left: var(--spacing-8);
        padding-right: var(--spacing-8);
      `)}
    `;
  }
  
  /**
   * 创建卡片样式
   */
  static card(options: {
    padding?: string;
    shadow?: keyof typeof DesignTokens.boxShadow;
    border?: boolean;
    hover?: boolean;
  } = {}): CSSResult {
    const {
      padding = 'var(--spacing-4)',
      shadow = 'base',
      border = true,
      hover = true
    } = options;
    
    return css`
      background: white;
      border-radius: var(--border-radius-md);
      padding: ${padding};
      box-shadow: var(--shadow-${shadow});
      
      ${border ? css`border: 1px solid var(--color-gray-200);` : css``}
      
      ${hover ? css`
        transition: all var(--duration-normal) var(--easing-smooth);
        
        &:hover {
          box-shadow: var(--shadow-lg);
          transform: translateY(-2px);
        }
      ` : css``}
    `;
  }
  
  /**
   * 创建按钮样式
   */
  static button(variant: 'primary' | 'secondary' | 'ghost' = 'primary', size: 'sm' | 'md' | 'lg' = 'md'): CSSResult {
    const sizeStyles = {
      sm: css`
        padding: var(--spacing-2) var(--spacing-3);
        font-size: var(--font-size-sm);
        height: 32px;
      `,
      md: css`
        padding: var(--spacing-3) var(--spacing-4);
        font-size: var(--font-size-base);
        height: 40px;
      `,
      lg: css`
        padding: var(--spacing-4) var(--spacing-6);
        font-size: var(--font-size-lg);
        height: 48px;
      `
    };
    
    const variantStyles = {
      primary: css`
        background: var(--color-primary-500);
        color: white;
        border: 1px solid var(--color-primary-500);
        
        &:hover:not(:disabled) {
          background: var(--color-primary-600);
          border-color: var(--color-primary-600);
        }
        
        &:active:not(:disabled) {
          background: var(--color-primary-700);
          border-color: var(--color-primary-700);
        }
      `,
      secondary: css`
        background: white;
        color: var(--color-gray-700);
        border: 1px solid var(--color-gray-300);
        
        &:hover:not(:disabled) {
          background: var(--color-gray-50);
          border-color: var(--color-gray-400);
        }
        
        &:active:not(:disabled) {
          background: var(--color-gray-100);
        }
      `,
      ghost: css`
        background: transparent;
        color: var(--color-gray-700);
        border: 1px solid transparent;
        
        &:hover:not(:disabled) {
          background: var(--color-gray-100);
        }
        
        &:active:not(:disabled) {
          background: var(--color-gray-200);
        }
      `
    };
    
    return css`
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: var(--border-radius-base);
      font-weight: var(--font-weight-medium);
      cursor: pointer;
      transition: all var(--duration-fast) var(--easing-smooth);
      user-select: none;
      
      ${sizeStyles[size]}
      ${variantStyles[variant]}
      
      &:focus-visible {
        outline: 2px solid var(--color-interaction-focus);
        outline-offset: 2px;
      }
      
      &:disabled {
        opacity: 0.6;
        cursor: not-allowed;
      }
    `;
  }
  
  /**
   * 创建输入框样式
   */
  static input(): CSSResult {
    return css`
      width: 100%;
      padding: var(--spacing-3);
      border: 1px solid var(--color-gray-300);
      border-radius: var(--border-radius-base);
      font-size: var(--font-size-base);
      line-height: var(--line-height-normal);
      background: white;
      transition: all var(--duration-fast) var(--easing-smooth);
      
      &:focus {
        outline: none;
        border-color: var(--color-primary-500);
        box-shadow: var(--shadow-focus);
      }
      
      &:disabled {
        background: var(--color-gray-50);
        cursor: not-allowed;
      }
      
      &::placeholder {
        color: var(--color-gray-400);
      }
    `;
  }
  
  /**
   * 创建文本样式
   */
  static text(options: {
    size?: keyof typeof DesignTokens.typography.fontSize;
    weight?: keyof typeof DesignTokens.typography.fontWeight;
    color?: string;
    align?: 'left' | 'center' | 'right';
  } = {}): CSSResult {
    const {
      size = 'base',
      weight = 'normal',
      color = 'var(--color-gray-900)',
      align = 'left'
    } = options;
    
    return css`
      font-size: var(--font-size-${size});
      font-weight: var(--font-weight-${weight});
      color: ${color};
      text-align: ${align};
      line-height: var(--line-height-normal);
    `;
  }
  
  /**
   * 创建动画样式
   */
  static animate(options: {
    duration?: keyof typeof DesignTokens.animation.duration;
    easing?: keyof typeof DesignTokens.animation.easing;
    properties?: string[];
  } = {}): CSSResult {
    const {
      duration = 'normal',
      easing = 'smooth',
      properties = ['all']
    } = options;
    
    return css`
      transition: ${properties.join(', ')} var(--duration-${duration}) var(--easing-${easing});
    `;
  }
  
  /**
   * 创建网格样式
   */
  static grid(columns: number | string, gap: string = 'var(--spacing-4)'): CSSResult {
    const gridColumns = typeof columns === 'number' 
      ? `repeat(${columns}, 1fr)` 
      : columns;
      
    return css`
      display: grid;
      grid-template-columns: ${gridColumns};
      gap: ${gap};
    `;
  }
  
  /**
   * 创建 Flexbox 样式
   */
  static flex(options: {
    direction?: 'row' | 'column';
    align?: 'start' | 'center' | 'end' | 'stretch';
    justify?: 'start' | 'center' | 'end' | 'between' | 'around' | 'evenly';
    gap?: string;
    wrap?: boolean;
  } = {}): CSSResult {
    const {
      direction = 'row',
      align = 'stretch',
      justify = 'start',
      gap = '0',
      wrap = false
    } = options;
    
    const alignItems = {
      start: 'flex-start',
      center: 'center',
      end: 'flex-end',
      stretch: 'stretch'
    };
    
    const justifyContent = {
      start: 'flex-start',
      center: 'center',
      end: 'flex-end',
      between: 'space-between',
      around: 'space-around',
      evenly: 'space-evenly'
    };
    
    return css`
      display: flex;
      flex-direction: ${direction};
      align-items: ${alignItems[align]};
      justify-content: ${justifyContent[justify]};
      gap: ${gap};
      ${wrap ? css`flex-wrap: wrap;` : css``}
    `;
  }
  
  /**
   * 创建截断文本样式
   */
  static truncate(lines: number = 1): CSSResult {
    if (lines === 1) {
      return css`
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      `;
    }
    
    return css`
      display: -webkit-box;
      -webkit-line-clamp: ${lines};
      -webkit-box-orient: vertical;
      overflow: hidden;
    `;
  }
  
  /**
   * 创建视觉隐藏样式（用于屏幕阅读器）
   */
  static visuallyHidden(): CSSResult {
    return css`
      position: absolute !important;
      width: 1px !important;
      height: 1px !important;
      padding: 0 !important;
      margin: -1px !important;
      overflow: hidden !important;
      clip: rect(0, 0, 0, 0) !important;
      white-space: nowrap !important;
      border: 0 !important;
    `;
  }
}
```

### 3. 全局样式基础

```css
/* 文件: packages/components/src/shared/global-styles.css */

/* CSS 变量注入 */
:root {
  /* 从设计令牌生成的变量 */
  --color-primary-50: #eff6ff;
  --color-primary-100: #dbeafe;
  --color-primary-500: #3b82f6;
  --color-primary-600: #2563eb;
  --color-primary-700: #1d4ed8;
  --color-primary-900: #1e3a8a;
  
  --color-gray-50: #f9fafb;
  --color-gray-100: #f3f4f6;
  --color-gray-200: #e5e7eb;
  --color-gray-300: #d1d5db;
  --color-gray-400: #9ca3af;
  --color-gray-500: #6b7280;
  --color-gray-600: #4b5563;
  --color-gray-700: #374151;
  --color-gray-800: #1f2937;
  --color-gray-900: #111827;
  
  --color-success: #22c55e;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
  --color-info: #3b82f6;
  
  --color-interaction-hover: rgba(59, 130, 246, 0.1);
  --color-interaction-active: rgba(59, 130, 246, 0.2);
  --color-interaction-focus: #3b82f6;
  --color-interaction-disabled: rgba(107, 114, 128, 0.3);
  --color-interaction-selected: rgba(59, 130, 246, 0.15);
  
  --spacing-0: 0px;
  --spacing-1: 4px;
  --spacing-2: 8px;
  --spacing-3: 12px;
  --spacing-4: 16px;
  --spacing-5: 20px;
  --spacing-6: 24px;
  --spacing-8: 32px;
  --spacing-10: 40px;
  --spacing-12: 48px;
  --spacing-16: 64px;
  --spacing-20: 80px;
  --spacing-24: 96px;
  
  --font-size-xs: 12px;
  --font-size-sm: 14px;
  --font-size-base: 16px;
  --font-size-lg: 18px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 30px;
  
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;
  
  --line-height-tight: 1.25;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;
  
  --border-radius-none: 0px;
  --border-radius-sm: 4px;
  --border-radius-base: 6px;
  --border-radius-md: 8px;
  --border-radius-lg: 12px;
  --border-radius-xl: 16px;
  --border-radius-full: 9999px;
  
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-base: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  --shadow-inner: inset 0 2px 4px 0 rgba(0, 0, 0, 0.06);
  --shadow-focus: 0 0 0 3px rgba(59, 130, 246, 0.5);
  
  --duration-fast: 150ms;
  --duration-normal: 300ms;
  --duration-slow: 500ms;
  
  --easing-smooth: cubic-bezier(0.4, 0, 0.2, 1);
  --easing-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
  --easing-sharp: cubic-bezier(0.4, 0, 0.6, 1);
  
  --z-hide: -1;
  --z-auto: auto;
  --z-base: 0;
  --z-docked: 10;
  --z-dropdown: 1000;
  --z-sticky: 1100;
  --z-banner: 1200;
  --z-overlay: 1300;
  --z-modal: 1400;
  --z-popover: 1500;
  --z-skip-link: 1600;
  --z-toast: 1700;
  --z-tooltip: 1800;
}

/* 暗色主题 */
@media (prefers-color-scheme: dark) {
  :root {
    --color-gray-50: #1f2937;
    --color-gray-100: #374151;
    --color-gray-200: #4b5563;
    --color-gray-300: #6b7280;
    --color-gray-400: #9ca3af;
    --color-gray-500: #d1d5db;
    --color-gray-600: #e5e7eb;
    --color-gray-700: #f3f4f6;
    --color-gray-800: #f9fafb;
    --color-gray-900: #ffffff;
    
    --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.3);
    --shadow-base: 0 1px 3px 0 rgba(0, 0, 0, 0.4), 0 1px 2px 0 rgba(0, 0, 0, 0.3);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.4), 0 2px 4px -1px rgba(0, 0, 0, 0.3);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.4), 0 4px 6px -2px rgba(0, 0, 0, 0.3);
    --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.4), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
  }
}

/* 高对比度模式 */
@media (prefers-contrast: high) {
  :root {
    --color-primary-500: #0066cc;
    --color-gray-300: #666666;
    --color-gray-700: #333333;
    --shadow-focus: 0 0 0 3px #000000;
  }
}

/* 减弱动画模式 */
@media (prefers-reduced-motion: reduce) {
  :root {
    --duration-fast: 0.01s;
    --duration-normal: 0.01s;
    --duration-slow: 0.01s;
  }
  
  *,
  *::before,
  *::after {
    animation-duration: 0.01s !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01s !important;
  }
}

/* 基础重置样式 */
*,
*::before,
*::after {
  box-sizing: border-box;
}

* {
  margin: 0;
}

html,
body {
  height: 100%;
}

body {
  line-height: var(--line-height-normal);
  -webkit-font-smoothing: antialiased;
  font-family: Inter, system-ui, sans-serif;
  color: var(--color-gray-900);
  background: var(--color-gray-50);
}

img,
picture,
video,
canvas,
svg {
  display: block;
  max-width: 100%;
}

input,
button,
textarea,
select {
  font: inherit;
}

p,
h1,
h2,
h3,
h4,
h5,
h6 {
  overflow-wrap: break-word;
}

/* 无障碍辅助 */
.sr-only {
  position: absolute !important;
  width: 1px !important;
  height: 1px !important;
  padding: 0 !important;
  margin: -1px !important;
  overflow: hidden !important;
  clip: rect(0, 0, 0, 0) !important;
  white-space: nowrap !important;
  border: 0 !important;
}

/* 焦点管理 */
.focus-trap {
  position: relative;
}

/* 工具类 */
.container {
  width: 100%;
  margin-left: auto;
  margin-right: auto;
  padding-left: var(--spacing-4);
  padding-right: var(--spacing-4);
}

@media (min-width: 640px) {
  .container {
    padding-left: var(--spacing-6);
    padding-right: var(--spacing-6);
  }
}

@media (min-width: 1024px) {
  .container {
    padding-left: var(--spacing-8);
    padding-right: var(--spacing-8);
  }
}
```

## 🎯 B3 立即执行的任务

### 1. 创建文件结构 (10分钟)
```bash
mkdir -p packages/components/src/shared
touch packages/components/src/shared/design-tokens.ts
touch packages/components/src/shared/style-utils.ts
touch packages/components/src/shared/global-styles.css
```

### 2. 复制上述代码到对应文件 (10分钟)

### 3. 集成到 LayoutSwitcher 组件 (15分钟)
```typescript
// 在 layout-switcher.ts 中导入和使用
import { StyleUtils } from '../shared/style-utils.js';

static styles = [
  css`${StyleUtils.card({ shadow: 'base', hover: true })}`,
  css`
    .layout-button {
      ${StyleUtils.button('secondary', 'md')}
      ${StyleUtils.interactive()}
    }
  `
];
```

### 4. 创建主题切换功能 (20分钟)
```typescript
// 文件: packages/components/src/shared/theme-manager.ts
export class ThemeManager {
  private static instance: ThemeManager;
  
  static getInstance(): ThemeManager {
    if (!this.instance) {
      this.instance = new ThemeManager();
    }
    return this.instance;
  }
  
  setTheme(theme: 'light' | 'dark' | 'auto') {
    document.documentElement.setAttribute('data-theme', theme);
    
    if (theme === 'auto') {
      const mediaQuery = window.matchMedia('(prefers-color-scheme: dark)');
      this.applyAutoTheme(mediaQuery.matches);
      mediaQuery.addEventListener('change', (e) => this.applyAutoTheme(e.matches));
    }
  }
  
  private applyAutoTheme(isDark: boolean) {
    document.documentElement.classList.toggle('dark', isDark);
  }
}
```

## 🚨 B3 需要注意的集成点

1. **与 B1 组件的协调**：确保样式变量与组件属性一致
2. **与 C2 响应式的协调**：使用统一的断点系统
3. **与我的交互设计的协调**：保持交互状态样式一致

**B2 实时支援状态：✅ 代码就绪**  
*B3 可以立即开始实施样式系统！*