# 分享访问流程与错误处理（功能小节）

> 关联上级：`工作空间/分享与访问控制.md`

## 1. 访问流程

- 用户访问 `/share/:workspaceId/:pageId`；
- 前端解析参数并重定向到对应 workspace/page；
- 如需登录，则先跳转登录页，登录成功后再回跳到原页面。

## 2. 错误场景

- 链接已过期 → 跳转到 `/expired` 并提示；
- 用户无权限 → 提示无权限，建议联系管理员；
- 页面已删除 → 提示页面不存在，提供返回工作空间首页入口。

