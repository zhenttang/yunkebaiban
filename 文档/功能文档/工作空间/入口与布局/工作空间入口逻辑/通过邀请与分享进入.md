# 通过邀请与分享进入（功能小节）

> 关联上级：`工作空间/入口与布局/工作空间入口逻辑.md`

## 1. 通过邀请链接进入

- 访问 `/invite/:inviteId`；
- 校验邀请是否有效；
- 未登录时先引导登录；
- 接受邀请后加入对应工作空间并跳转到默认页面。

## 2. 通过分享链接进入

- 访问 `/share/:workspaceId/:pageId`；
- 解析目标工作空间与页面；
- 根据分享权限决定是否需要登录或加入工作空间；
- 最终重定向到 `/workspace/:workspaceId/:pageId`。

